<h1>Editing Normal note</h1>

<%= render 'form', :normalnote => @normalnote, :url => normalnote_path(@normalnote.id) %>

<%= link_to 'Show', @normalnote %> |
<%= link_to 'Back', notebook_path(@normalnote.notebook_id) %>
